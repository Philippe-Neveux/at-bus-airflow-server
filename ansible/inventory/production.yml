---
# Production Inventory
# This file is dynamically updated by GitHub Actions workflow
# For local development, update these values manually
all:
  children:
    airflow_servers:
      hosts:
        airflow-prod:
          ansible_host: 35.244.73.100
          ansible_user: at-bus-airflow-server